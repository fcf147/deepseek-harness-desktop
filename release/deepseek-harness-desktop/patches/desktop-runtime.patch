# desktop-runtime.patch
#
# 本补丁在 rc.8 基线（repo/deepseek-harness-master = 官方 deepseek-harness 0.1.0-rc.8）下已不再需要。
#
# 早期（rc.5 基线）曾在此对官方 apps/cli/package.json 补充 19 个
# @deepseek-ai/* workspace 依赖闭包，并对 pnpm-workspace.yaml 放宽 peer 范围，
# 以适配桌面版 hoisted 布局下的 pnpm deploy。升级到官方 0.1.0-rc.8 后，
# 官方源码的依赖闭包与 peer 范围均已自洽，桌面版可直接 pnpm deploy 解析，
# 无需对 repo/deepseek-harness-master/ 打任何补丁。
#
# 保留此空壳仅为追溯历史；不要将其应用到 rc.8 源码树。
