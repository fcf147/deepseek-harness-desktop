# 2026-08-20

## 在 yuanbanjiake 分支移除全部自定义插件
- 目标：仅保留官方 harness + 桌面壳。删除 6 个第三方/自定义插件（dsh-remote、dshmarket、dsh-message-edit、@dsh-external/dsh-vision-toolkit、dsh-memory-evolve、dsh-wsl-workspace）、dsh-mcp-client 桥接、summer-craft 预设 + dsh-code-review/dsh-doc-standards 技能、repo/vendor/dsh-memory-evolve 离线快照。
- 已提交并推送 Gitee（commit 8d7a1f2，178 files changed, 65 insertions, 61318 deletions）。
- 同步更新 README/LICENSE/THIRD_PARTY_NOTICES/补丁说明，以及 desktop 与 release 两份 main.js、prepare-runtime.mjs；移除 scripts/setup.mjs 中 dsh-memory-evolve 本地 git 镜像死代码；desktop-runtime.patch 精简为仅官方依赖闭包改动。
- 待确认：release/CHANGELOG.md 与 RELEASE-0.1.0/0.1.2.md 仍含历史插件描述（作为版本发布记录保留，未改）；apps/cli 的 @deepseek-ai/dsh-mcp-client 已从 package.json 与 packages/mcp/mcp-client 移除（用户确认其为自定义桥接）；pnpm-lock.yaml 含旧条目，构建时需重新 pnpm install 重建。

## 同步官方 harness 基线 0.1.0-rc.5 → 0.1.0-rc.8
- repo/deepseek-harness-master/ 整体替换为官方 dsh-v0.1.0-rc.8（commit 141eb6f）完整源码快照（tar 排除 .git，diff -rq 字节级一致）；rc.8 依赖闭包/peer 自洽，无需任何补丁，旧 rc.5 的 19 个 @deepseek-ai/* 闭包补丁作废；dsh-mcp-client 在 rc.8 是官方包（workspace:^）已随基线恢复。
- 已提交推送（909d268，1786 files，+65213/-11918）。README/THIRD_PARTY_NOTICES/release 补丁说明均改标 rc.8；desktop-runtime.patch 改为历史空壳。
- 验证（全部通过）：pnpm install && pnpm run build 从零构建成功（5m44s，245 工程，lockfile 未被改动）；prepare-runtime --platform win32 交叉组装成功（pnpm deploy + vendored 还原 + 符号链接展开，deployed dsh = 0.1.0-rc.8）。后又提交 c2f9afd 修正 3 处过期注释（setup.mjs 头部、prepare-runtime 的"5 个第三方插件"与 supportedArchitectures 描述）。
- 遗留发现（非同步引入）：① npm tar 7.5.22 无法解 xz（TAR_BAD_ARCHIVE）且 WSL 系统无 xz 命令 → build:linux 的 linux runtime 组装在本机必挂，Windows 路径用 zip+extract-zip 不受影响；② 官方 rc.8 pnpm-workspace.yaml 无 supportedArchitectures，Linux 上交叉构建 win32 产物只带 linux 原生模块，正式 Windows 包应在 Windows 构建机产出。
- 技术教训（WSL 坑）：wsl.exe 会把多行参数拍平成一行、外层单引号在边界被剥掉 → 命令内禁用 $VAR 与嵌套引号，一律内联绝对路径；需要引号/多行的逻辑写脚本文件再执行。

## Node.js 集成按平台重构（commit d7d5d9b）
- 需求：①Windows 内置"最新符合 harness engines（^22.19.0 || >=24.0.0）"的 Node 二进制、封装进安装包、安装期自动识别系统 Node 状态并后台静默补齐后再装 harness；②Linux 不内置 Node、安装/运行时按发行版提示补全；③鸿蒙不动。
- 实现：prepare-runtime 新增 resolveNodeVersion（抓 nodejs.org index.json，过滤 engines 取最新 LTS=当前 v24.19.0 Krypton；DSH_DESKTOP_NODE_VERSION 可固定；fetchWithRetry 3 次退避抗 WSL 抖动）；Windows node 改为 extraResources 直接打进安装包（7z 只留 dsh），installer.nsh 安装期检测系统 node --version（v22./v24./v25./v26. 前缀启发式）写 .use-system-node 标记；main.js 重写 resolveNode（标记→系统 Node 严格 semver 校验、不满足回退内置；Linux 无内置→系统 Node，缺失按 /etc/os-release 弹 apt/dnf/pacman/zypper 提示，重试循环）；electron-builder.yml win 加 node extraResources、linux filter !node/**；release/ 层 7 个文件同步镜像（顺带补回 release/main.js 缺失的 preset 选择功能）。
- 验证：win32 prepare-runtime 动态解析成功（选择 v24.19.0 LTS → 下载 → pnpm 交叉 shim 全通过，20s）；main.js 纯逻辑单测 12/12（版本判定）+ 10/10（发行版识别，补了 ol/oracle 分支）；node --check 全过。runtime/node/win32-x64 现为 v24.19.0（131MB）。
- 遗留注意：Linux 无内置 Node 后 dsh 插件命令的 pnpm 需用户环境提供（corepack/PATH）；WSL 上 nodejs.org 偶发 ETIMEDOUT（fetch 已加重试，实测 curl 通、node fetch 偶挂）。

## 确认 Linux(wine) 交叉构建 Windows release（commit 6346e45）
- 结论：**可行**。wine 11.0(Staging) 已装 → electron-builder 在 Linux 上构建 Windows 时默认要 wine 跑 rcedit 注入 exe 图标/版本资源（不装可 signAndEditExecutable=false 跳过）；NSIS 用 linux makensis 不需要 wine。
- 关键堵点修复（此前实测 win32 deploy 只有 linux 原生模块）：①repo/pnpm-workspace.yaml 追加 supportedArchitectures（os:[current,win32]），pnpm install 后 store 有 18 个 win32 平台包（koffi-win32-x64、sharp-win32-x64 等，claude/codex 大包被排除表跳过）；②deploy 闭包缺 @deepseek-ai/cordis-plugin-group（dsh-app-boot 的 peer，auto-install-peers=false 不装；--version 在 dev 短路不暴露）→ restoreVendoredOverrides 加 vendor/group 还原。
- 完整 build:win 实测成功（3m54s）：prepare-runtime（对齐/补齐 win32 平台包 4 个，含从 registry 下载版本对齐的 koffi-win32-x64@3.1.5）→ electron-builder（NSIS，签名跳过）→ dist/DeepSeek Harness-0.1.2-win-setup.exe（113.8MB 含内置 Node）+ dsh-runtime.7z（30MB 仅 dsh，解压 286M）。
- wine 冒烟验证：win32 node.exe v24.19.0 跑 dsh --version → 0.1.0-rc.8；koffi 3.1.5 / sharp win32 原生模块加载 OK；7z 解压后同样通过。wine 下 node 的 stdio EBADF 怪癖：stdin/stdout/stderr 都要重定向到文件/设备（`< /dev/null > file 2>&1`）才能稳定跑。
- 注意：Windows 安装需 exe + dsh-runtime.7z 同目录（NSIS 用 7za 解压归档）。

## 单文件安装包改造（commit e0793e2）
- 用户澄清：dsh-runtime.7z 拆分只为 Gitee release 100MB 附件限制，无其他用意；希望产物入仓、不单独 release 上传。
- 但 Gitee 免费仓库单文件上限 50MB（官方帮助中心 2025 表格：免费版单仓库 500MB/单文件 50MB；旧文档 100MB），比 release 附件（100MB）更严；HTTP push 单次 200MB 上限。
- 改造：dsh 运行时改为 extraResources 直接打进安装包（win extraResources 加 runtime/dsh），build.mjs 删除 7za 复制与 7z 打包步骤、installer.nsh 删除归档解压（保留 Node 检测标记）、删除 desktop/tools/7za.exe（未跟踪的构建产物）、README/文档更新；release 层 4 文件镜像。
- 实测单文件 exe = 168.6MB（自包含 Electron + Node v24.19.0 + dsh 完整闭包，2m07s 构建成功）。
- 交付通道未定（待用户拍板）：168.6MB 超 Gitee 免费仓库 50MB/文件 → 产物入仓不可行。选项：GitHub Release（2GB/文件，免费）/ Gitee 升级套餐 / 网盘 / 7z 分卷。

## 技术注意
- Git 操作必须经 wsl.exe -d FedoraLinux-44 -- bash -c "..."；Git Bash 下 /wsl$ 未挂载。Read/Edit 工具可直接用 //wsl$/FedoraLinux-44/...。
- bash heredoc 内写含反引号的内容会被当作命令替换，篡改 python 源；用 <<'PYEOF' 单引号定界符避免。含全角括号/反引号的行用 python re.sub(r"[^
]*") 整行替换最稳。
